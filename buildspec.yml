version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11

  build:
    commands:
      - echo "Build started"
      - cd lab11
      - mvn test
      - mvn package

artifacts:
  files:
    - lab11/target/*.jar