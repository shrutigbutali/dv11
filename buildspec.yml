version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11

  build:
    commands:
      - echo "Build started"
      - mvn test
      - mvn package

artifacts:
  files:
    - target/*.jar